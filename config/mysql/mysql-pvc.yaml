apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql
  namespace: testjin
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
      #storageClassName: nfs-client
  volumeMode: Filesystem
